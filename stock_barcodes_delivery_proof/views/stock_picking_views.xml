<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Extend picking form view -->
    <record id="view_picking_delivery_proof_form" model="ir.ui.view">
        <field name="name">stock.picking.delivery.proof.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">

            <!-- Add Delivery Proof page -->
            <xpath expr="//page[last()]" position="before">
                <page
                    name="delivery_proof"
                    string="Delivery Proof"
                    invisible="not show_delivery_proof"
                >
                    <field name="show_delivery_proof" invisible="1" />

                    <!-- Info message -->
                    <div class="alert alert-info" role="alert">
                        <p>
                            <i class="fa fa-info-circle" />
                            Delivery proof photos are captured per move line during the barcode scanning process.
                            This tab shows only move lines that have at least one photo.
                        </p>
                    </div>

                    <!-- Display only move lines with photos -->
                    <group string="Proof Photos">
                        <field
                            name="move_lines_with_photos"
                            nolabel="1"
                            colspan="2"
                            context="{'tree_view_ref': 'stock_barcodes_delivery_proof.view_move_line_delivery_proof_tree'}"
                        >
                            <tree>
                                <field name="product_id" />
                                <field name="lot_id" optional="show" />
                                <field name="quantity" />
                                <field name="product_uom_id" />
                                <field name="delivery_proof_count" string="Photos" />
                                <button
                                    name="action_open_line_photos"
                                    type="object"
                                    string="View Photos"
                                    icon="fa-camera"
                                />
                            </tree>
                        </field>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Tree view for move lines showing photos -->
    <record id="view_move_line_delivery_proof_tree" model="ir.ui.view">
        <field name="name">stock.move.line.delivery.proof.tree</field>
        <field name="model">stock.move.line</field>
        <field name="arch" type="xml">
            <tree>
                <field name="product_id" />
                <field name="lot_id" optional="show" />
                <field name="quantity" />
                <field name="product_uom_id" />
                <field name="delivery_proof_count" string="Photos" />
                <button
                    name="action_open_line_photos"
                    type="object"
                    string="View Photos"
                    icon="fa-camera"
                    invisible="delivery_proof_count == 0"
                />
            </tree>
        </field>
    </record>
</odoo>
